#include "System.h"
#include "User.h"
void System::SignUp(User user) {
    /*if (users.find(user.id) == users.end()) {
        users[user.id] = new User(user);
    }
    else {
        cout << "User already exists" << endl;
    }*/
}
void System::Login(int ID,string password) {
    /*if (users.find(ID) == users.end()) {
        cout << "User is not Registered to system" << endl << "Sign Up " << endl;
    }
    else {
        if (GetPassword(users.find(ID)) == password) {
            if (users.find(ID).isAdmin==true) {
                cout<<"Admin Signed In"
            }
            else {
                cout << "User Signed In" << endl;
            }
        }
        else {
            cout << "Invalid National ID or Password" << endl;
        }
    }*/
}